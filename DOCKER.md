# 🐳 Docker документация

**Роль: DevOps** - контейнеризация и деплой приложения

## 📦 Docker образ

Приложение упаковано в Docker контейнер для удобного развёртывания.

## 🚀 Быстрый старт

### Запуск через docker-compose (рекомендуется)

```bash
# Запустить все сервисы (бот + веб)
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### Запуск отдельных сервисов

```bash
# Только Telegram бот
docker-compose up bot

# Только веб-приложение
docker-compose up web
```

### Сборка образа вручную

```bash
# Сборка образа
docker build -t debt-bot .

# Запуск бота
docker run -d \
  --name debt-bot \
  --env-file .env \
  -v $(pwd)/debts.db:/app/debts.db \
  debt-bot \
  python src/main.py

# Запуск веб-приложения
docker run -d \
  --name debt-web \
  -p 5000:5000 \
  -v $(pwd)/debts.db:/app/debts.db \
  debt-bot \
  python src/web/app.py
```

## 📁 Volumes

БД монтируется как volume для сохранения данных между перезапусками:

```yaml
volumes:
  - ./debts.db:/app/debts.db
  - ./data:/app/data
```

## 🔧 Переменные окружения

Создайте файл `.env`:

```env
BOT_TOKEN=your_bot_token_here
```

Файл автоматически загружается через `env_file` в docker-compose.yml.

## 🌐 Порты

- **5000** - веб-приложение (Flask)
- Telegram бот работает через webhook или polling (не требует портов)

## 🔄 Обновление

```bash
# Пересобрать образы
docker-compose build

# Перезапустить с новыми образами
docker-compose up -d
```

## 🧹 Очистка

```bash
# Остановить и удалить контейнеры
docker-compose down

# Удалить образы
docker-compose down --rmi all

# Полная очистка (включая volumes)
docker-compose down -v
```

## 📊 Мониторинг

```bash
# Статус контейнеров
docker-compose ps

# Использование ресурсов
docker stats

# Логи бота
docker-compose logs bot

# Логи веб-приложения
docker-compose logs web
```

## 🐛 Отладка

```bash
# Запуск в интерактивном режиме
docker-compose run --rm bot bash

# Выполнение команд внутри контейнера
docker-compose exec bot python -c "from src.database import Database; db = Database(); print(db.get_statistics())"
```

## 🧪 Тестовое окружение

Для запуска тестов в Docker используется отдельный compose файл `docker-compose.test.yml`:

```bash
# Запуск всех тестов в Docker
./scripts/run_tests.sh

# Или вручную
docker-compose -f docker-compose.test.yml up --build test-runner
```

**Особенности тестового окружения:**
- Отдельная тестовая БД (`test_debts.db`) для изоляции тестов
- Веб-приложение для тестов на порту 5002
- Healthcheck для веб-приложения перед запуском тестов
- Автоматическая генерация coverage отчёта

Подробнее в [TESTING.md](TESTING.md)

## 🚢 Production

Для production рекомендуется:

1. Использовать multi-stage build для уменьшения размера образа
2. Настроить health checks
3. Использовать secrets management
4. Настроить reverse proxy (nginx)
5. Использовать orchestration (Kubernetes, Docker Swarm)

