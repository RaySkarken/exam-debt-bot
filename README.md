# 💰 СКОЛЬКО Я ДОЛЖЕН ВАСЕ (или пятничная драма)

Telegram-бот для отслеживания долгов за совместные покупки (пицца, еда, и т.д.)

🔗 **Репозиторий**: https://github.com/RaySkarken/exam-debt-bot

## 🎯 Проблема

Пятница, 18:00. Офис. Дедлайн. Вася заказывает пиццу на всех. 4200 рублей.

- Кто сколько должен?
- Кто уже заплатил?
- Кто забыл заплатить?
- Сколько дней прошло с момента покупки?

**Хаос.** Бот решает это.

## 🚀 Возможности

### Основной функционал
- 📝 Создание расходов с распределением по участникам
- 💸 Отметка о выплате долга
- 📊 Просмотр всех долгов
- ⏰ Отслеживание просроченных долгов
- 👥 Статистика по участникам
- 📜 История операций (кто когда что делал)
- 📋 Детали расхода (кто должен, кто заплатил)
- 📦 Группировка долгов по расходам
- ❌ Отмена расхода (только создатель)

### 🎨 Интерфейс Telegram бота
- **Inline кнопки** — интерактивное меню для всех действий
- **Кнопки выплаты** — погашение долга одним нажатием
- **Подтверждение действий** — безопасные операции с подтверждением
- **FSM для создания расходов** — пошаговый мастер через кнопки
- **Только кнопки** — удобный интерфейс без текстовых команд

### 🌐 Веб-приложение (в разработке)
- Веб-интерфейс для управления долгами
- REST API для интеграции
- Синхронизация с Telegram ботом
- Современный UI/UX

## 📦 Установка

```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd exam

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

## 🔧 Настройка

1. Создать бота через [@BotFather](https://t.me/BotFather)
2. Получить токен
3. Создать файл `.env`:
```
BOT_TOKEN=your_bot_token_here
```

## ▶️ Запуск

### Telegram бот

```bash
python src/main.py
```

### Веб-приложение

```bash
# Запустить Flask сервер
python src/web/app.py

# Или через Flask CLI
flask --app src.web.app run
```

Веб-приложение будет доступно по адресу: `http://localhost:5000`

## 📖 Использование

### Основные команды
- `пицца 4200 @Петя @Маша @Коля @Оля @Дима` — создать расход
- `скинул Васе 700` — отметить выплату
- `долги` — показать все долги
- `долги @Вася` — долги конкретному человеку
- `долги по расходам` — долги сгруппированные по расходам

### Дополнительные команды
- `расход пицца` — детали расхода (кто должен, кто заплатил)
- `история` — история всех операций
- `история пицца` — история конкретного расхода
- `отменить пицца` — отменить расход (только создатель)
- `статистика` — общая статистика
- `статистика @Петя` — статистика по пользователю

## 🧪 Тестирование

```bash
# Запустить все тесты
pytest

# Запустить с подробным выводом
pytest -v

# Запустить BDD тесты
pytest tests/features/

# Запустить тесты веб-приложения
pytest tests/web/
```

## 📁 Структура проекта

```
exam/
├── README.md           # Этот файл
├── CLAUDE.md           # Описание работы с AI
├── requirements.txt    # Зависимости
├── .env                # Токен бота (не коммитить!)
├── src/
│   ├── main.py         # Точка входа Telegram бота
│   ├── bot.py          # Логика бота
│   ├── database.py     # Работа с БД
│   ├── keyboards.py    # Клавиатуры для бота
│   ├── web/            # Веб-приложение
│   │   ├── app.py      # Flask приложение
│   │   ├── api.py      # REST API
│   │   └── templates/  # HTML шаблоны
│   │       └── index.html
│   │       └── static/  # CSS, JS
│   └── __init__.py
├── tests/
│   ├── features/       # BDD .feature файлы
│   ├── steps/          # Step definitions
│   └── web/            # Тесты веб-приложения
└── .gitignore
```

## 🛠️ Технологии

### Telegram бот
- Python 3.10+
- aiogram 3.24.0
- SQLite
- pytest-bdd

### Веб-приложение
- Flask (backend)
- HTML/CSS/JavaScript (frontend)
- REST API
- SQLite (общая БД с ботом)

## 📝 Лицензия

MIT

