# 🤖 CLAUDE.md - Описание работы с AI

Этот документ описывает процесс разработки проекта с использованием AI (Claude) в рамках подхода Vibe Coding.

## 🎯 Подход

Проект разрабатывался с использованием **BDD (Behavior-Driven Development)** как спецификации и **AI как соавтора** на всех этапах.

## 📋 Этапы работы

### 1. Анализ и планирование
- Изучение требований из EXAM_PROJECTS.md
- Выбор проекта (#2 - "СКОЛЬКО Я ДОЛЖЕН ВАСЕ")
- Определение ролей (Аналитик, Архитектор, Разработчик, Тестировщик, Техписатель, DevOps)

### 2. BDD спецификация (Аналитик)
- Написание .feature файлов с описанием поведения системы
- Определение сценариев использования
- Описание Given-When-Then шагов

### 3. Архитектура (Архитектор)
- Проектирование структуры БД
- Определение модулей и их взаимодействия
- Выбор паттернов проектирования

### 4. TDD разработка (Разработчик + Тестировщик)
- Написание step definitions для pytest-bdd
- Реализация бизнес-логики по принципу "красный-зелёный-рефакторинг"
- Интеграция с aiogram

### 5. Документация (Техписатель)
- Написание README.md
- Описание работы с AI в CLAUDE.md
- Инструкции по запуску

## 🤝 Взаимодействие с AI

### Запросы к AI:
- "Напиши BDD сценарий для функции создания расхода"
- "Спроектируй схему БД для отслеживания долгов"
- "Реализуй функцию по TDD подходу"
- "Напиши step definition для pytest-bdd"
- "Помоги с рефакторингом кода"

### Роль AI:
- Соавтор в написании кода
- Помощник в проектировании
- Рецензент архитектуры
- Генератор тестовых сценариев

## 📊 Метрики

- Количество коммитов: 10+
- Покрытие тестами: основные сценарии
- BDD файлов: 3-5 .feature файлов
- Время разработки: 3 часа

## 🎓 Выводы

AI помог:
- Быстро сгенерировать BDD спецификации
- Реализовать код по TDD
- Написать документацию
- Соблюсти best practices

Человек обеспечил:
- Понимание бизнес-логики
- Принятие архитектурных решений
- Контроль качества
- Коммиты и эволюцию проекта

## 🎨 Эволюция: Добавление кнопок и улучшение UX

### Этап 2: Интеграция Telegram возможностей

**Проблема:** Текстовые команды неудобны для пользователей

**Решение:** Добавлены кнопки и интерактивный интерфейс

**Роли команды:**
1. **Аналитик** - проанализировал UX, определил структуру кнопок, написал BDD сценарии
2. **Архитектор** - спроектировал структуру меню, клавиатур, callback handlers
3. **Разработчик** - реализовал:
   - Модуль `keyboards.py` с функциями создания клавиатур
   - Inline keyboard для главного меню
   - Callback handlers для всех действий
   - Reply keyboard для быстрого доступа
   - FSM для пошагового создания расходов
4. **Техписатель** - обновил документацию

**Реализовано:**
- ✅ Inline кнопки для всех основных действий
- ✅ Reply keyboard с быстрыми командами
- ✅ Интерактивные кнопки для выплаты долгов
- ✅ Подтверждение критических действий
- ✅ Пошаговый мастер создания расходов
- ✅ Главное меню с навигацией

**Коммиты:**
- `feat: добавлена структура кнопок и меню` - архитектура и BDD
- `feat: реализованы обработчики кнопок и callback` - интеграция

**Результат:** Бот стал намного удобнее в использовании, все функции доступны через кнопки

### Этап 3: Упрощение интерфейса - только кнопки

**Проблема:** Конфликт между текстовыми командами и кнопками, reply keyboard мешал

**Решение:** Полный переход на inline кнопки

**Роли команды:**
1. **Разработчик** - убрал reply keyboard, отключил текстовые команды
2. **Тестировщик** - проверил работу всех кнопок
3. **Техписатель** - обновил документацию

**Реализовано:**
- ✅ Убран reply keyboard полностью
- ✅ Отключены текстовые команды (кроме FSM)
- ✅ Все функции только через inline кнопки
- ✅ Улучшена обработка ошибок редактирования сообщений
- ✅ Кнопка "Главное меню" на каждом шаге FSM

**Коммиты:**
- `refactor: упрощена логика обработки сообщений, приоритет кнопкам`
- `refactor: убраны reply keyboard и текстовые команды, только inline кнопки`

**Результат:** Чистый интерфейс, нет конфликтов, все работает через кнопки

### Этап 4: Веб-приложение

**Цель:** Создать веб-интерфейс для управления долгами

**Роли команды:**
1. **Архитектор** - спроектировал структуру веб-приложения, API endpoints, файловую структуру
2. **Аналитик** - написал BDD сценарии для веб-приложения (web_app.feature)
3. **Разработчик** - реализовал:
   - Flask backend с REST API (app.py, api.py)
   - HTML/CSS/JavaScript фронтенд (index.html, style.css, app.js)
   - Интеграция с существующей БД
   - Модальные окна для создания расходов и выплат
4. **Тестировщик** - написал тесты для REST API (test_api.py)
5. **Техписатель** - обновил документацию, добавил инструкции по запуску

**Реализовано:**
- ✅ Flask backend с REST API
- ✅ Современный responsive фронтенд
- ✅ Вкладки: Долги, Расходы, Статистика, История
- ✅ Создание расходов через веб-интерфейс
- ✅ Выплата долгов через веб-интерфейс
- ✅ Просмотр статистики и истории
- ✅ Интеграция с общей БД (SQLite)
- ✅ Тесты для всех API endpoints

**API Endpoints:**
- `GET /api/debts` - список долгов
- `GET /api/expenses` - список расходов
- `POST /api/expenses` - создать расход
- `POST /api/payments` - выплатить долг
- `GET /api/statistics` - статистика
- `GET /api/history` - история операций
- `GET /api/debts/grouped` - долги по расходам

**Коммиты:**
- `feat: архитектура и BDD сценарии для веб-приложения` - архитектура и BDD
- `feat: создан Flask backend с REST API` - backend
- `feat: создан фронтенд веб-приложения` - frontend
- `test: добавлены тесты для REST API` - тесты
- `docs: добавлена документация по запуску веб-приложения` - документация

**Результат:** Полнофункциональное веб-приложение с современным UI, работающее с той же БД что и Telegram бот

**Проверка работы веб-приложения:**
После запуска в Docker было проведено тестирование веб-интерфейса в браузере:
- ✅ Страница успешно загружается на http://localhost:5001
- ✅ Все API endpoints работают корректно:
  - `GET /api/debts` - возвращает 5 активных долгов (200 OK)
  - `GET /api/statistics` - возвращает статистику (общая сумма: 6400₽, 5 долгов)
  - `GET /api/history` - возвращает историю операций (200 OK)
  - `GET /api/expenses` - возвращает список расходов (200 OK)
- ✅ Интерфейс отображается корректно:
  - Навигация с 4 вкладками (Долги, Расходы, Статистика, История)
  - Отображение 5 активных долгов с кнопками "💸 Выплатить"
  - Модальные окна для создания расхода и выплаты долга
  - Кнопки обновления данных работают
- ✅ JavaScript загружает данные при открытии страницы
- ✅ CSS стили применяются корректно
- ✅ Все основные функции доступны и работают

**Вывод:** Веб-приложение полностью функционально, все компоненты работают корректно, готово к использованию

### Этап 5: Docker контейнеризация

**Цель:** Упростить развёртывание и обеспечить консистентность окружения

**Роли команды:**
1. **DevOps** - создал Docker контейнеризацию:
   - Dockerfile для приложения
   - docker-compose.yml для оркестрации сервисов
   - .dockerignore для оптимизации образа
   - Настройка volumes для БД
   - Конфигурация переменных окружения
2. **Техписатель** - создал DOCKER.md с полной документацией

**Реализовано:**
- ✅ Dockerfile с оптимизированным образом
- ✅ docker-compose.yml для запуска бота и веб-приложения
- ✅ .dockerignore для уменьшения размера образа
- ✅ Volumes для сохранения БД между перезапусками
- ✅ Поддержка переменных окружения через .env
- ✅ Документация по использованию Docker

**Преимущества:**
- Единое окружение для всех разработчиков
- Простое развёртывание на сервере
- Изоляция зависимостей
- Легкое масштабирование
- Удобный мониторинг и логирование

**Коммиты:**
- `feat: добавлена Docker контейнеризация` - Docker файлы
- `docs: обновлён README с информацией про Docker` - документация

**Результат:** Приложение полностью контейнеризировано, готово к развёртыванию через Docker

**Запуск в Docker:**
- ✅ Оба сервиса (бот и веб-приложение) успешно запущены в контейнерах
- ✅ Веб-приложение доступно на порту 5001 (порт изменён из-за конфликта с macOS)
- ✅ Бот работает в контейнере и принимает сообщения
- ✅ Общая БД (SQLite) используется обоими сервисами через volumes
- ✅ Все изменения закоммичены и запушены в GitHub

### Этап 6: Обновление тестов и улучшение покрытия кода

**Цель:** Привести тесты в соответствие с текущим функционалом и увеличить покрытие кода

**Проблема:** 
- Тесты использовали устаревшие текстовые команды, которые были удалены
- Низкое покрытие кода (26%)
- BDD тесты не запускались
- Отсутствовали unit-тесты для ключевых модулей

**Роли команды:**
1. **Тестировщик** - выполнил полное обновление тестов:
   - Обновил все BDD сценарии для работы с кнопками
   - Создал unit-тесты для keyboards.py (5 тестов)
   - Создал unit-тесты для bot.py (8 тестов)
   - Создал расширенные тесты для database.py (10 тестов)
   - Настроил pytest-bdd в pytest.ini
   - Исправил ошибки в тестах веб-приложения
   - Увеличил общее покрытие с 26% до 45%

**Реализовано:**
- ✅ Обновлены все BDD feature файлы:
  - `create_expense.feature` - создание через кнопки и FSM
  - `pay_debt.feature` - выплата через кнопки
  - `keyboard_ui.feature` - убран reply keyboard
  - `view_debts.feature` - просмотр через кнопки
  - `statistics.feature` - статистика через кнопки
- ✅ Созданы новые step definitions:
  - `test_keyboard_steps.py` - для работы с кнопками
  - `test_statistics_steps.py` - для статистики
- ✅ Добавлены unit-тесты:
  - `test_keyboards.py` - 5 тестов, покрытие 94%
  - `test_bot.py` - 8 тестов, покрытие 49%
  - `test_database_extended.py` - 10 тестов, покрытие 67%
- ✅ Настроен pytest-bdd для автоматического обнаружения feature файлов
- ✅ Исправлены ошибки импорта в тестах веб-приложения

**Результаты покрытия кода:**

| Модуль | До | После | Изменение |
|--------|-----|-------|-----------|
| `keyboards.py` | 0% | 94% | +94% |
| `bot.py` | 6% | 49% | +43% |
| `database.py` | 55% | 67% | +12% |
| `web/api.py` | 72% | 72% | — |
| `web/app.py` | 75% | 75% | — |
| **Общее** | **26%** | **45%** | **+19%** |

**Статистика тестов:**
- Всего тестов: 29
- Пройдено: 29 (100%)
- Провалено: 0
- Время выполнения: ~1.7 сек

**Коммиты:**
- `test: обновлены тесты в соответствии с текущим функционалом` - обновление BDD
- `test: обновлены оставшиеся тесты для работы с кнопками` - финальное обновление
- `test: добавлены unit-тесты и настроен pytest-bdd` - новые тесты
- `fix: исправлен тест выплаты долга и настройка pytest-bdd` - исправления

**Результат:** Тесты полностью соответствуют текущему функционалу, покрытие кода увеличено почти в 2 раза, все тесты проходят успешно

### Этап 7: E2E тесты фронтенда + тестовое окружение в Docker (изолированная БД)

**Цель:** Добавить автоматические E2E тесты для веб-интерфейса и запускать весь тестовый набор в Docker, используя изолированную тестовую БД.

**Проблема:**
- Тесты запускались локально, без гарантированно одинакового окружения
- Не было тестов на фронтенд (взаимодействие UI в браузере)
- Нужна изоляция данных: тесты не должны трогать боевую БД

**Роли команды:**
1. **Тестировщик**:
   - Добавил E2E тесты веб-интерфейса на Playwright: `tests/web/test_e2e.py`
   - Подготовил фикстуры Playwright в `tests/conftest.py`
2. **DevOps**:
   - Добавил отдельный compose для тестов: `docker-compose.test.yml`
   - Добавил healthcheck для `test-web`, чтобы `test-runner` запускался только после готовности сервиса
   - Добавил скрипты для запуска тестов: `scripts/run_tests.sh`, `scripts/run_tests_local.sh`
3. **Разработчик**:
   - Добавил поддержку тестовой БД через переменную окружения `DATABASE_PATH` в `src/web/api.py`
4. **Техписатель**:
   - Создал отдельную документацию: `TESTING.md`
   - Обновил `README.md` (как запускать тесты локально и в Docker)

**Реализовано:**
- ✅ Playwright + pytest-playwright добавлены в зависимости
- ✅ E2E тесты фронтенда: загрузка страницы, вкладки, модалки, отправка формы, проверка API
- ✅ Docker тестовый стенд: `test-db`, `test-web` (порт 5002), `test-runner`
- ✅ Тестовая БД изолирована (в Docker: `DATABASE_PATH=/app/data/test_debts.db`)
- ✅ Автоматическая генерация coverage (HTML отчёт в `htmlcov/`)

**Прогон тестов в Docker (фактический результат):**
- Собрано и запущено через `docker-compose -f docker-compose.test.yml up --build test-runner`
- Всего тестов: 37
- Пройдено: 34
- Упало: 3 (все 3 — E2E тесты фронтенда)
- Покрытие кода: 45% (HTML отчёт записан в `htmlcov/`)

**Причины падений E2E (наблюдения):**
- `test_create_expense_modal`: не найден `#expense-modal h3` (селектор/структура DOM отличается)
- `test_create_expense_form`: strict-mode конфликт из-за двух `input[name="amount"]` (расход + выплата)
- `test_refresh_buttons`: попытка клика по кнопке, которая не видима (вкладка не активна/элемент скрыт CSS)

**Коммиты:**
- `feat: добавлены E2E тесты фронтенда и Docker окружение для тестов (роли: Тестировщик, DevOps, Разработчик)`
- `fix: исправлено дублирование test-web в docker-compose.test.yml (роль: DevOps)`
- `fix: исправлен путь к тестовой БД в docker-compose.test.yml (роль: DevOps)`

**Исправление упавших E2E тестов:**
После первого прогона были исправлены 3 упавших теста:
1. **test_create_expense_modal** - исправлен селектор с `h3` на `h2` (соответствует HTML шаблону)
2. **test_create_expense_form** - уточнены селекторы через `#expense-form` для избежания конфликта с формой выплаты
3. **test_refresh_buttons** - добавлены уточняющие селекторы через `#debts-tab` и `#statistics-tab` для клика по видимым кнопкам
4. **test_pay_debt_modal** - исправлен селектор с `h3` на `h2` для модального окна выплаты

**Результат после исправлений:**
✅ **Все 37 тестов проходят успешно!** (проверено запуском в Docker)
- Все E2E тесты работают корректно
- Покрытие кода: 45%
- Время выполнения: 8.37 секунд
- Тесты проверяют реальное поведение веб-интерфейса в браузере

### Этап 8: Аудит безопасности и исправление уязвимостей

**Цель:** Провести аудит безопасности и исправить уязвимости уровня выше среднего

**Роли команды:**
1. **Security Auditor** - провёл аудит безопасности, выявил уязвимости
2. **Разработчик** - исправил обнаруженные уязвимости

**Обнаруженные уязвимости:**
1. **XSS (Cross-Site Scripting)** - использование `innerHTML` с данными из API без экранирования
2. **Отсутствие валидации входных данных** - нет проверки типов, диапазонов, санитизации
3. **Debug режим в production** - `debug=True` включен всегда
4. **Деление на ноль** - отсутствие проверки пустого списка participants
5. **Отсутствие ограничений на размер данных** - нет лимитов на длину строк и значения

**Исправления:**
- ✅ Создана функция `escapeHtml()` для экранирования HTML в JavaScript
- ✅ Заменено использование `innerHTML` на безопасное создание элементов через DOM
- ✅ Добавлена функция `validate_input()` для валидации всех входных данных API
- ✅ Добавлена функция `sanitize_string()` для санитизации строк
- ✅ Исправлен debug режим - включается только в development
- ✅ Добавлена проверка на деление на ноль в `create_expense()`
- ✅ Добавлены ограничения: длина строк (description: 200, creator: 50), количество participants (100), максимальный amount (1 млрд)
- ✅ Улучшена обработка ошибок - не раскрываются внутренние детали

**Файлы изменены:**
- `src/web/static/js/app.js` - исправлены XSS уязвимости
- `src/web/api.py` - добавлена валидация входных данных
- `src/web/app.py` - исправлен debug режим
- `src/database.py` - добавлена проверка на деление на ноль

**Результат:** Все уязвимости уровня выше среднего исправлены, приложение стало значительно безопаснее

**Документация:** Создан файл `SECURITY_FIXES.md` с подробным описанием всех исправлений (не коммитится в GitHub по запросу)

