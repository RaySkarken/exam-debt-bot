# 🌐 Архитектура веб-приложения

**Роль: Архитектор**

## 🎯 Цель

Создать веб-интерфейс для управления долгами, который использует ту же БД что и Telegram бот.

## 📐 Архитектура

### Backend (Flask)
- **app.py** - основное Flask приложение
- **api.py** - REST API endpoints
- Использует существующий модуль `database.py`
- JSON API для фронтенда

### Frontend
- **index.html** - главная страница
- **static/css/style.css** - стили
- **static/js/app.js** - JavaScript логика
- Современный responsive дизайн

### API Endpoints

```
GET  /api/debts              - список всех долгов
GET  /api/debts/<username>   - долги пользователя
GET  /api/expenses           - список расходов
GET  /api/expenses/<id>      - детали расхода
POST /api/expenses           - создать расход
POST /api/payments           - выплатить долг
GET  /api/statistics         - статистика
GET  /api/history            - история операций
```

### Структура файлов

```
src/web/
├── app.py              # Flask приложение
├── api.py              # REST API
├── templates/
│   └── index.html      # Главная страница
└── static/
    ├── css/
    │   └── style.css   # Стили
    └── js/
        └── app.js      # JavaScript
```

## 🔄 Интеграция

- Общая БД (SQLite) с Telegram ботом
- Одинаковая бизнес-логика
- Синхронизация в реальном времени (через общую БД)

