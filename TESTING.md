# 🧪 Тестирование

Документация по тестированию приложения управления долгами.

## 📋 Типы тестов

### 1. Unit тесты
- Тесты для отдельных модулей (`src/bot.py`, `src/database.py`, `src/keyboards.py`)
- Быстрые, изолированные тесты
- Используют временные БД

### 2. BDD тесты
- Тесты на основе сценариев (`.feature` файлы)
- Используют pytest-bdd
- Проверяют поведение системы с точки зрения пользователя

### 3. API тесты
- Тесты для REST API endpoints
- Проверяют корректность работы API

### 4. E2E тесты (End-to-End)
- Тесты веб-интерфейса с использованием Playwright
- Проверяют работу фронтенда в браузере
- Используют тестовую БД

## 🚀 Запуск тестов

### Локальный запуск (без Docker)

```bash
# Установка зависимостей
pip install -r requirements.txt

# Установка браузеров для Playwright
playwright install chromium

# Запуск всех тестов
pytest tests/ -v --cov=src --cov-report=html --cov-report=term

# Запуск только unit тестов
pytest tests/test_*.py -v

# Запуск только E2E тестов
pytest tests/web/test_e2e.py -v

# Запуск BDD тестов
pytest tests/features/ -v
```

### Запуск через скрипт (локально)

```bash
# Запуск всех тестов с веб-приложением
./scripts/run_tests_local.sh
```

### Запуск в Docker

```bash
# Запуск всех тестов в Docker с тестовой БД
./scripts/run_tests.sh

# Или вручную
docker-compose -f docker-compose.test.yml up --build
```

## 🗄️ Тестовая база данных

Для изоляции тестов используется отдельная тестовая БД:

- **Файл:** `test_debts.db`
- **Путь:** Указывается через переменную окружения `DATABASE_PATH`
- **Автоматическое создание:** БД создаётся автоматически при первом запуске тестов
- **Очистка:** БД очищается перед каждым запуском тестов в Docker

### Переменные окружения для тестов

```bash
export DATABASE_PATH=test_debts.db
export TEST_WEB_URL=http://localhost:5001
export FLASK_ENV=testing
```

## 📊 Покрытие кода

После запуска тестов с флагом `--cov`:

- **Терминал:** Отчёт в консоли
- **HTML:** Отчёт в `htmlcov/index.html`

Откройте `htmlcov/index.html` в браузере для детального просмотра покрытия.

## 🎯 Структура тестов

```
tests/
├── __init__.py
├── conftest.py              # Общие фикстуры
├── test_bot.py              # Unit тесты для bot.py
├── test_database_extended.py # Unit тесты для database.py
├── test_keyboards.py         # Unit тесты для keyboards.py
├── features/                # BDD feature файлы
│   ├── create_expense.feature
│   ├── pay_debt.feature
│   └── ...
├── steps/                   # Step definitions для BDD
│   ├── test_expense_steps.py
│   └── ...
└── web/                     # Тесты веб-приложения
    ├── test_api.py          # API тесты
    └── test_e2e.py          # E2E тесты фронтенда
```

## 🔧 Настройка тестового окружения

### Docker Compose для тестов

Файл `docker-compose.test.yml` содержит:

1. **test-db** - сервис для тестовой БД
2. **test-web** - веб-приложение для тестов (порт 5002)
3. **test-runner** - сервис для запуска тестов

### Healthcheck

Веб-приложение имеет healthcheck endpoint `/health` для проверки готовности перед запуском тестов.

## 📝 Написание новых тестов

### Unit тест

```python
def test_function():
    db = Database(db_path=":memory:")
    result = db.some_method()
    assert result == expected
```

### BDD тест

1. Создайте `.feature` файл в `tests/features/`
2. Добавьте step definitions в `tests/steps/`
3. Запустите: `pytest tests/features/your_feature.feature`

### E2E тест

```python
def test_web_feature(page: Page):
    page.goto("http://localhost:5001")
    page.locator('button[data-tab="debts"]').click()
    expect(page.locator("#debts-tab")).to_be_visible()
```

## 🐛 Отладка тестов

### Локальная отладка

```bash
# Запуск с выводом print
pytest tests/ -v -s

# Запуск конкретного теста
pytest tests/test_bot.py::test_function -v

# Запуск с остановкой на первой ошибке
pytest tests/ -x
```

### Отладка в Docker

```bash
# Просмотр логов
docker-compose -f docker-compose.test.yml logs test-runner

# Запуск интерактивной сессии
docker-compose -f docker-compose.test.yml run test-runner /bin/bash
```

## ✅ Best Practices

1. **Изоляция:** Каждый тест должен быть независимым
2. **Тестовая БД:** Используйте отдельную БД для тестов
3. **Очистка:** Очищайте данные после тестов
4. **Именование:** Используйте понятные имена тестов
5. **Покрытие:** Стремитесь к высокому покрытию кода

## 📈 Метрики

- **Всего тестов:** 29+
- **Покрытие кода:** ~45%
- **Время выполнения:** ~1.7 сек (локально), ~30 сек (Docker)

## 🔗 Полезные ссылки

- [pytest документация](https://docs.pytest.org/)
- [pytest-bdd документация](https://pytest-bdd.readthedocs.io/)
- [Playwright документация](https://playwright.dev/python/)

